# ProofRabbit（证明兔）路线图

这份路线图会明确区分“现在已经能用”和“以后计划接入”。证明兔不会在缺少可信平台或结算数据源的情况下，声称自己能够自动发现所有链上的全部 AI Agent 收入。

## 现在——Agent Tank 参赛版本

- 可公开访问的中英文网站，以及一次完成的验证流程。
- 索引 Base 上的 AntSeed 结算，包括带有买方签名的付款和通道最终结果。
- 每次开始验证时，从已经保存的位置继续读取到 Base 当前最新可用区块。
- 计算累计收入、付款钱包数量、最大付款方占比，以及结果稳定的收入可信度、欺诈风险和证据充分程度。
- 读取 AntSeed 完整性登记，并按需检查主要付款钱包的关联线索。
- 通过部署在 Studio Next 上的 `proofrabbit-revenue-v7` GenLayer 合约，为每个地址给出具体的中英文判断。
- 把与证据绑定的链上报告、引用的钱包和判断理由保存到合约，并公开合约与交易资料。
- 允许符合条件的被检查钱包领取不可转让、有效期 30 天的收入凭证，并提供公开核验页。

## 下一步——接入第二个生态数据源

- 把已经通过本地测试的 GH Bounty / Solana 适配器接入公开验证流程。
- 明确 GH Bounty 中哪些完成、付款、拒绝和退款事件可以作为证据。
- 把 GH Bounty 资料整理成同一套精简评估格式，但不会假装 Base 与 Solana 的记录完全相同。
- 增加可以重复验证的 GH Bounty 示例和完整网页流程测试。

## 稳定性——托管式索引服务

- 把发布前手动运行的完整账本更新，迁移成服务器定时运行的任务。
- 长期保存读取进度、历史事件和数据源版本。
- 增加失败重试、RPC 备用节点、数据新鲜度监控，以及容易理解的降级提示。
- 继续使用由证据内容生成的报告编号，确保相同证据永远对应同一个报告。

## 扩展——明确的跨平台适配器

- 先评估 OKX AI 的结算与任务完成证据；只有在数据能够被独立读取和核验时才正式接入。
- 通过各自的数据适配器继续增加其他 Agent 市场和链。
- 在不隐藏数据来源的前提下，把多个已支持平台的报告合并成一份持续更新的 Agent 收入档案。
- 为 Agent 市场和买方提供只读核验接口。

## 当前不会声称已经做到

- 自动发现一个 Agent 使用过的所有平台。
- 证明两个钱包一定属于同一个现实中的人。
- 把普通、没有平台签名或结算证据的钱包转账当成已经验证的 Agent 收入。
- 在每个数据源完成接入和测试之前，宣称已经支持所有平台。
