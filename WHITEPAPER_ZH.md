# ProofRabbit（证明兔）— AI Agent 可验证收入证据

版本：`proofrabbit-revenue-v7`
凭证标准：`proofrabbit-revenue-attestation-v3`
网络：GenLayer Studio Next（Chain ID 61997）
状态：可公开使用的产品原型

## 摘要

AI Agent 可以对外展示收入、客户数量和历史成果，但钱包里有钱，不代表这些钱真的来自客户。普通转账、关联钱包互转，以及收入过度集中在单一付款方，都可能让一个 Agent 看起来比实际情况更可靠。

ProofRabbit 会把平台里的买方签名结算整理成一份清楚的收入证据报告。固定规则负责计算可以重复验证的数字；GenLayer 智能合约负责根据这个地址的具体证据，给出一段有明确理由的中文和英文判断。通过判断的 Agent 钱包可以领取一份不可转让、带有效期、可以直接在链上核验的收入凭证。

## 1. 要解决的问题

顾客或 Agent 平台在相信一个 Agent 之前，真正需要知道的是：它宣称的收入，看起来是不是来自真实客户和真实业务？

下面三种常见做法都不够：

- 只看钱包余额，分不清业务收入、充值和自己转给自己。
- 只看交易次数，不能证明付款钱包彼此独立。
- 只看截图或徽章，无法确认资料有没有过期，也无法核验发行方当前保存的记录。

因此，ProofRabbit 不把钱包里的所有进账都当成收入，而是先寻找平台结算证据。

## 2. 当前产品能做什么

公开产品目前支持 Base 上的 AntSeed 结算。系统保存 AntSeed 历史事件账本，并在用户开始验证时，从上次记录的位置继续读取较新的区块。它会识别带有买方签名的结算，然后计算：

- 累计结算收入；
- 付款钱包数量；
- 最大付款方占总结算金额的比例；
- 已完成、未付款和退回未使用资金的通道结果；
- AntSeed 完整性登记里记录的异常结算；
- 在公开浏览器资料可用时，主要付款钱包之间的直接转账和共同资金来源线索；
- 收入可信度、欺诈风险和证据充分程度。

仓库里还包含一个已通过本地测试的 GH Bounty / Solana 资料适配器，但它尚未接入公开判断流程。当前版本不会宣称已经支持所有平台。

## 3. 证据怎样形成

### 3.1 先找平台结算，不把普通转账当收入

ProofRabbit 会把业务结算事件与无关的钱包转账分开。以 AntSeed 为例，只有公开链上通道事件能够还原结算，而且记录中带有买方签名时，这笔钱才会计入已经验证的平台收入。

### 3.2 数值先由固定规则计算

索引器会生成一个精简证据包。三个分数由统一规则计算，所以证据不变时，结果也不会随机改变。GenLayer 验证者不需要重新猜测收入、客户数量或交易次数。

### 3.3 检查钱包关联线索

系统可以检查主要付款钱包之间是否有直接转账，或者是否来自同一个资金源。这些是风险线索，不能单凭一项线索就断言两个钱包一定属于同一个人。产品只说明实际观察到的事实，不假装知道链下真实身份。

### 3.4 报告与证据一一对应

每个报告编号都来自标准化证据内容的 SHA-256 摘要。如果报告编号与提交的证据不一致，合约会拒绝处理。这样，公开判断会和当时被检查的那一份具体证据绑定在一起。

## 4. GenLayer 在产品里做什么

GenLayer 不会重新计算已经由固定规则算好的收入数字。主验证者先根据同一份精简事实，写出针对这个地址的具体结论、说明和风险点；其他验证者独立判断同一份证据，并对真正决定结果的 `risk_profile`（风险档案）和 `primary_risk`（主要风险）进行投票。它们不会各自生成另一套分数，也不会要求几段不同措辞的回答逐字相同。

合约会保存：

- 验证者接受的中英文具体分析；
- 支持结论的原因代码；
- 判断中引用的钱包地址；
- 当时被判断的完整证据；
- 规则版本与证据版本。

网站会直接显示合约保存的中英文结论，并把分数和底层证据放在下面。对于收入集中度较高但没有钱包关联证据的地址，结论会明确说明收入依赖单一付款方，而不会把集中度本身误判成欺诈。

## 5. 链上收入凭证

当某个地址获得合格判断后，被检查的钱包本人可以领取 ProofRabbit 收入凭证。这份凭证：

- 与被检查的钱包绑定，不能转让；
- 只能由该钱包本人领取；
- 有效期为 30 天；
- 可以由钱包本人撤销；
- 同一个钱包领取新版后，旧版会自动标记为已被替代；
- 任何人都可以直接从 GenLayer 合约核验。

当前领取门槛为：合约给出 `no_fraud_signals` 风险档案、收入可信度至少 70、欺诈风险不高于 30、证据充分程度至少 70。高风险或证据不足的报告仍会留在链上供人核验，但不能包装成正面的收入凭证。

## 6. 安全与完整性

v7 合约会拒绝格式错误的资料、不支持的规则版本、彼此冲突的可信度和风险数据、过大的证据包、重复使用的报告编号，以及和证据摘要不一致的报告编号。它会把模型输出限制在证据能够支持的判断类型内；如果模型回答格式错误，则采用由证据直接推导的备用分析，避免整笔交易因为文字格式问题失败。证据里的文字只会被当成资料，不会被当成指令执行。

网站连接钱包时只读取用户选择的公开地址，不会要求助记词或私钥，也不会请求无限代币授权。每一笔 Studio Next 写入交易都通过 GenLayer 官方 Transaction Kit 读取费用，并由用户在钱包中查看和亲自确认。

## 7. 数据更新与限制

公开产品把一份可携带的历史快照和按需读取的新区块合并起来。历史快照目前在发布前手动更新；正式产品应让索引器定时运行，并把完整账本保存到长期在线的服务器资料库。

ProofRabbit 不能证明链下真实身份，也不能在没有平台结算证据的情况下自动识别全世界所有 AI Agent 收入。每增加一个平台，都需要可信的数据接口、公开合约事件或专门的适配器。

## 8. 后续计划

完整、持续维护的中文路线图见 [`ROADMAP_ZH.md`](./ROADMAP_ZH.md)。简而言之：下一步把 GH Bounty / Solana 接入公开产品；随后建设托管式索引更新与监控；再为 OKX AI 和其他 Agent 市场增加明确的数据适配器。在每个平台拥有可靠的结算证据来源之前，ProofRabbit 不会宣称已经覆盖所有平台。

## 9. 当前部署

- 网站：https://agent-revenue-passport.manshiguang124.chatgpt.site
- 源码：https://github.com/TJC-66/agent-revenue-passport
- GenLayer 规则版本：`proofrabbit-revenue-v7`
- Studio Next 合约：`0x79ab7ac7a17920354547A0B0b1d8f955F76278CC`
- 成功部署交易：`0x36a61596567852d5ed693bdcadfb85961fa1e34704d4b12b733049b1452cb506`
- Studio Next 浏览器：https://explorer-studio-dev.genlayer.com/
- RPC：`https://studio-next.genlayer.com/api`

## 10. 可重复测试地址

| 风险等级 | Agent 钱包 | 预期结果 |
| --- | --- | --- |
| 低风险 | `0x4668854ba3e8b094e6f48fbeb59cec1cfde162f2` | 收入可信度 85、欺诈风险 15、证据充分程度 95 |
| 中等风险 | `0xbc33134b5f441ca1aa4a47e6e79f4e170285b75f` | 收入可信度 55、欺诈风险 45、证据充分程度 95 |
| 高风险 | `0x0329c5d3920e301740f78d6e17b8d1a11cca9b2c` | 收入可信度 5、欺诈风险 95、证据充分程度 95 |
